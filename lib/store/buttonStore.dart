import 'package:flutter/material.dart';

class ButtonStore extends ChangeNotifier {
  // final List<ListView> _items = [];
  var initialValue = false;

  // List get buttonBalue => _items;

  void changebuttonvalue() {
    initialValue;
  }
}
